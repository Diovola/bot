# Discord bot 核心套件
discord.py==2.4.1

# Flask 用於 Web server
Flask==2.3.3

# HTTP 請求工具
requests==2.31.0

# 時區處理（如果你的程式有用到）
pytz==2023.3

# 可選：定時任務用（如果有用到 time / schedule）
schedule==1.2.0
